#include <monC_python.h>


int main(int ac, char **argv)
{

/* if more than one argv, ac = 2, print error USAGE: monty file */
/* instead of stdin use fileio stuff
chk = getline(&input, &sz_input, stdin);
if (chk == -1)
{
	my_argv = NULL;
	break;
}
*/
if (access(file, R_OK));

while (getline() != EOF)
		getline(&input, &sz_input, file);
}